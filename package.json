{
  "scripts": {
    "start": "trunk serve",
    "build": "esbuild src/js/**/*.js --bundle --format=esm --minify --entry-names=[dir]/[name].min --outbase=src/js --outdir=esbuild/js"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "esbuild": "^0.20.2",
    "tailwindcss": "^3.4.3"
  },
  "dependencies": {
    "highlight.js": "^11.9.0",
    "prismjs": "^1.29.0"
  }
}
